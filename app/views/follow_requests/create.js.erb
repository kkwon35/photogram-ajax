var follow_request_button = $("<%= j(render("follow_requests/follow_unfollow", sender: @follow_request.sender, recipient: @follow_request.recipient))%>")

$("#follow_request_id").replaceWith(follow_request_button)
